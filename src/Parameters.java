//********************************************************
// Parameters.java
//
// This program illustrates the concept of **********
// **********************
// *****
//
//********************************************************
import java.util.*;
public class Parameters
{
    //-----------------------------------------------
    // Calls the average and minimum methods with
    // different numbers of parameters.
    public void  numAverage(int number){
        number/args.lenght;
        System.out.println(number);
    }
    public void NoParametersFunction(){
    }
    //System.out.println();
}

    //-----------------------------------------------
    public static void main(String[] args)
    {
        double mean1, mean2;
        mean1 = average(**************);
        mean2 = average(***********************);
        System.out.println ("mean1 = " + mean1);
        System.out.println ("mean2 = " + mean2);
    }

//----------------------------------------------
// Returns the average of its parameters.
//----------------------------------------------
/* Now specify the variable array 'list'
public static double average ( ********) (4 pts)
{
double result = 0.0;
if (list.length != 0)
{
int sum = 0;
for (int ********) //Use for-each (3 pts)
sum += num;
result = *******************; (5 pts)
}
return result;
}
}